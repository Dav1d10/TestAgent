# Stage 3: Receives and validates GitHub webhook events (push / pull_request).
# Validates the HMAC signature using GITHUB_WEBHOOK_SECRET before invoking the agent.
# Placeholder — implement in Stage 3.
